builds:
- env:
    - CGO_ENABLED=0
    - GOOS=linux
    - GOARCH=amd64
    - GORACE=
    - GOROOT=
    - GOPATH=
  binary: caculator
  goos:
  - linux
  goarch:
  - amd64
  ldflags: -s -w
  main: ./
  name_template: '{{ .Name }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}'
  folders:
  - dist
